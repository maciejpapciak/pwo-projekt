mutation register(
  $firstName: String!
  $lastName: String!
  $username: String!
  $email: EmailAddress!
  $password: String!
) {
  register(
    options: {
      firstName: $firstName
      lastName: $lastName
      username: $username
      email: $email
      password: $password
    }
  ) {
    username
  }
}
